<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewBag.Title - مكتبة الكترونية</title>
    @Styles.Render("~/Content/css")
    @Scripts.Render("~/bundles/modernizr")
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.8.3/font/bootstrap-icons.css">
      <link href="~/Content/bootstrap-icons.css" rel="stylesheet" />
</head>
<body>
<script></script>
    <div class="navbar navbar-inverse navbar-fixed-top">
        <div class="container">
            @Html.ActionLink("مكتبة الكترونية", "Index", "Home", new { area = "" }, new { @class = "navbar-brand pull-right" })
            <div class="navbar-header">
                <button type="button" class="navbar-toggle pull-left" data-toggle="collapse" data-target=".navbar-collapse">
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>

                </button>
                @*@Html.Partial("_NotificationPartial")*@

            </div>

            <div class="navbar-collapse collapse navbartabs">

                <ul class="nav navbar-nav pull-right">
                    @if (User.IsInRole("Admin"))
                    {
                        <li>@Html.ActionLink("لوحة التحكم", "AdminDashboard", "Admin")</li>

                    }
                    <li>@Html.ActionLink("اتصل بنا", "Contact", "Home")</li>
                    <li>@Html.ActionLink("بحث", "Search", "Home")</li>
                    <li>@Html.ActionLink("التصنيفات", "CategoriesIndex", "Home")</li>
                    <li>@Html.ActionLink("الكتب", "BooksIndex", "Home")</li>
                </ul>
                @Html.Partial("_LoginPartial")
            </div>
        </div>
    </div>
    <div class="container body-content">

        @RenderBody()
        <hr />
        <footer>
            <p>&copy; @DateTime.Now.Year - مكتبة الكترونية</p>
        </footer>
    </div>
<script src="~/Scripts/jquery-{version}.js"></script>
<script src="~/Scripts/bootstrap.js"></script>
<script src="~/Scripts/bootbox.js"></script>
<script src="~/Scripts/bootbox.min.js"></script>
<script src="~/Scripts/DataTables/dataTables.bootstrap.js"></script>
<script src="~/Scripts/jquery.dataTables.js"></script>
<script src="~/Scripts/toastr.js"></script>
<script src="~/Scripts/toastr.min.js"></script>
<script src="~/Scripts/WaitingUntilLoadingPage.js"></script>
<script src="~/Scripts/PreviewSelectedImage.js"></script>
<script src="~/Scripts/sweetalert.min.js"></script>
<script src="~/Scripts/underscore-umd-min.js"></script>

    @Scripts.Render("~/bundles/lib")
    @RenderSection("scripts", required: false)


    <!--<script type="text/x-template" id="notifications-template">
        <ul class="notifications">
            <%
            _.each(notifications, function(notification) {-->
            @*if(notification.type == 1) { %>
                <li><span class="highlight"><%= notification.post.Profile.FirstName %></span> has cancelled the gig at <%= notification.post.Id %> at <%= moment(notification.post.DateAdded).format("D MMM HH:mm") %>.</li>
                <%}*@

            <!--if(notification.type == 3) { %>
            <li><span class="highlight"><%= notification.post.Profile.FirstName  %></span> has created a post at <%= notification.post.Id %> at <%= moment(notification.post.DateAdded).format("D MMM HH:mm") %>.</li>
            <%}
            })
            %>
        </ul>

    </script>

    <script>
        $(document).ready(function () {

            $.getJSON("/api/Notifications", function (notifications) {

                if (notifications.length == 0)
                    return;


                $(".js-notification-count")
                    .text(notifications.length)
                    .removeClass("hide")
                    .addClass("animate__animated animate__shakeX");

                $(".notifications").popover({

                    html: true,
                    title: "Notifications",
                    content: function () {
                        var compiled = _.template($("#notifications-template").html());
                        return compiled({ notifications: notifications });
                    },
                    placement: "bottom"
                });
            });

        });
    </script>-->


</body>
</html>
